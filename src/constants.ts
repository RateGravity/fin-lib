/**
 * Multiply years by this number to get the number of months
 */
export const MonthsPerYear = 12;

/**
 * Divide by this number to convert a yearly rate in percent to
 * a monthly decimal rate
 */
export const MonthlyRateDecimal = MonthsPerYear * 100;
